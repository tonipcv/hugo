env_name: LunarLander-v2
algorithm: dqn
experiment_name: lunarlander_dqn

training:
  total_timesteps: 200000
  learning_rate: 0.0001
  batch_size: 32
  gamma: 0.99
  use_gpu: true
  seed: 42
  log_interval: 5000
  eval_interval: 20000
  eval_episodes: 10

output_dir: ./outputs
use_wandb: false
